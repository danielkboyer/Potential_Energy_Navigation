#include "Properties.h"

 
Properties::Properties(float friction, float travelDistance,long maxAgentCount, float agentStartX, float agentStartY,float gravity):friction(friction),travelDistance(travelDistance),gravity(gravity),
maxAgentCount(maxAgentCount),agentStartX(agentStartX),agentStartY(agentStartY){

}

